export default {
    search(state) {
        return state.search;
    },
    searchList(state) {
        return state.searchList;
    },
    currentSearch(state) {
        return state.currentSearch;
    },
}